<%- include('../partials/header', { title: 'Edit Product' }) %>

<h2 class="mb-3">Edit Product</h2>

<form action="/products/edit/<%= product._id %>" method="POST" class="w-50">
  <div class="mb-3">
    <label class="form-label">Name</label>
    <input type="text" class="form-control" name="name" value="<%= product.name %>" required>
  </div>
  <div class="mb-3">
    <label class="form-label">Address</label>
    <input type="text" class="form-control" name="address" value="<%= product.address %>">
  </div>
  <div class="mb-3">
    <label class="form-label">Phone</label>
    <input type="text" class="form-control" name="phone" value="<%= product.phone %>">
  </div>
  <div class="mb-3">
    <label class="form-label">Supplier</label>
    <select class="form-select" name="supplierId" required>
      <% suppliers.forEach(s => { %>
      <option value="<%= s._id %>" <%= product.supplierId.equals(s._id) ? 'selected' : '' %>><%= s.name %></option>
      <% }) %>
    </select>
  </div>
  <button type="submit" class="btn btn-success">Update</button>
  <a href="/products" class="btn btn-secondary">Cancel</a>
</form>

<%- include('../partials/footer') %>